require 'torch'
require 'nn'

-- load training data
-- train_data = load(training_set)

-- define network
-- network = define_network

-- train network
-- train(train_data, network)

-- load testing data
-- test_data = load(testing_set)

-- test network
-- results = test(test_data, network)
